context("Metatests for project")

# devtools:::find_code does not work during tests.
# test_that("Test that all files have a license header", {
#     cat(license_header("sensorweb4R"))
#     expect_true(has_license_header(), info = license_header())
# })
